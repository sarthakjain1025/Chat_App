<%- include('layouts/header.ejs') %>

<div class="container">
    <div>
        <img src="/<%= group.image %>" alt="" width="200px" height="200px">
        <p><b><%= group.name %></b></p>

        <p><b>Total Members: </b> <%= totalMembers %></p>

        <% 
        if(available !=0){ 
        %>
        <p>Available for <b><%= available %></b> Members Only.</p>
        <% 
    }
    
    if(isOwner)
    {
        %>
        <p>This group is created by You.</p>
        <% 
    }
    else if(isJoined>0)
    {
        %>
        <p>You are already added to this group.</p>
        <% 
    }
    else if(available == 0)
    {
        %>
        <p>The Group is already Full !!</p>
        <% 
    }
    else{
        %>
        <button class="btn btn-primary"> Join now</button>
        <%
    }
        
        %>
    </div>

</div>

<%- include('layouts/footer.ejs') %>